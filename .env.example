# SSH接続用の環境変数設定例
# このファイルをコピーして.envにリネームし、適切な値を設定してください

# === 必須設定 ===
# SSH接続先のホスト名またはIPアドレス
UBUNTU_SSH_HOST=192.168.1.100

# SSH接続ポート（通常は22）
UBUNTU_SSH_PORT=22

# SSH接続用のユーザー名
UBUNTU_SSH_USERNAME=your_username

# === 認証設定（どちらか一方を設定） ===

# 方法1: パスワード認証
UBUNTU_SSH_PASSWORD=your_password

# 方法2: 秘密鍵認証（パスワード認証より推奨）
# 秘密鍵ファイルのフルパス（~/.ssh/id_rsaなど）
# UBUNTU_SSH_PRIVATE_KEY_PATH=/path/to/your/private/key
# 秘密鍵にパスフレーズが設定されている場合のみ
# UBUNTU_SSH_PRIVATE_KEY_PASSPHRASE=your_passphrase_if_needed

# === 設定のヒント ===
# - Windowsの場合: パスの区切りは / または \\ を使用
# - 秘密鍵のパス例: C:/Users/YourName/.ssh/id_rsa
# - セキュリティのため、秘密鍵認証を推奨します
